import React from 'react';
import './spinner.scss';

const Spinner = () => {
    return (
      <div className="spin">
        <div className="spinner"></div>
      </div>
    );
}

export default Spinner;
