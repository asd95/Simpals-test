const initialState = {
  title: "",
  body: "",
  tags: []
};

const formReducer = (state = initialState, action) => {
    switch (action.type) {
        case "": 
            
            break;
        default: return state
            
    }
}

export default formReducer;